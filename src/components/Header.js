import React from 'react';

const Header = (props) => {
    return (
        <div>
            <h1>MBA API - An open source api for standardized testing</h1>
        </div>
    )
}

export default Header;
